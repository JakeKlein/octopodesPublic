modelPredict = function(modelName, testData, modelVersion = 1){
  #Get model predictions
  if(any(is.na(modelVersion))){
    modelVersion = 1;
  }
  tes
  #Add to model environment
  #Return results
}
